interface:
  display_name: "Git Commit Message Generator"
  short_description: "Draft commit messages from the current git diff only."
  default_prompt: "Use $git-commit-msg-generator to inspect the current git changes and draft standard and detailed commit messages without committing anything."
